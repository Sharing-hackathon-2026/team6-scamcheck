// Cấu hình frontend. Stage 1: KHÔNG có key nào ở đây.
// API_BASE = '' nghĩa là cùng origin (prod qua Nginx proxy /api/* → Flask,
// hoặc dev khi phục vụ frontend từ chính backend ở :5000).
export const API_BASE = '';
